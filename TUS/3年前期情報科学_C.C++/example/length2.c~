
  #include <stdio.h>

  int string_length( char *str ) {
    int i;

    i = 0;
    while( str[i] != 0 ) { 
      printf("%s\n" , str[i]);
      i++;
    }

    return i;
  }




  main () {
    char string[8];

    scanf( "%s" , string );
  
    printf( "%d\n" , string_length( string ) );
  }
